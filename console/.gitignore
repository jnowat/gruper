# Dependencies
node_modules/

# SvelteKit / Vite build output
.svelte-kit/
build/
.vite/

# Tauri / Rust build output
src-tauri/target/
src-tauri/gen/

# Staged sidecar binaries (scripts/build-desktop.sh/.ps1) — build artifacts,
# not source; platform-specific and must not be committed.
src-tauri/binaries/

# Logs & local env
*.log
.env
.env.*
!.env.example

# Editor / OS noise
.DS_Store
Thumbs.db
