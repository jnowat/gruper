{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Default capability — allows store plugin access for auth token persistence.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "store:default"
  ]
}
