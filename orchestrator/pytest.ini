[pytest]
asyncio_mode = auto
testpaths = tests
# Add the repo root to sys.path so "from orchestrator.main import app" resolves
# whether pytest is run from the repo root or from inside orchestrator/.
pythonpath = ..
